
<h1>Marketplace</h1>

@foreach (var offer in Model.AllOffers)
{
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">@offer.Title</h5>
           @*  <a href="@Url.Action("Details", "Marketplace", new { id = offer.Id })" class="btn btn-primary">Details</a> *@
        </div>
    </div>
}
```